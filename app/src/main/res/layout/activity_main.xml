<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:canvas=".MainActivity">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/static_background"
        android:contentDescription="static background to stop zooming of fractal view revealing the brick wall at the back of theatre" />

    <view
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        class="jetsetilly.mandelbrot.RenderCanvas.RenderCanvas"
        android:id="@+id/fractalView"
        android:cropToPadding="false"
        />

    <view
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        class="jetsetilly.mandelbrot.ProgressView"
        android:src="@mipmap/ic_progress"
        android:id="@+id/progressView"
        android:layout_gravity="end|bottom"
        android:visibility="invisible"
        android:elevation="10dp"
        android:background="@drawable/progress_background"
        android:layout_margin="20dp"
        android:padding="7dp"
        android:cropToPadding="false"
        android:clickable="false"
        android:baselineAlignBottom="false"
        android:adjustViewBounds="false"
        />

    <view
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        class="jetsetilly.mandelbrot.MandelbrotActionBar"
        android:id="@+id/toolbar"
        android:layout_gravity="start|top"
        android:minHeight="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:visibility="invisible" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:id="@+id/info_pane"
        android:layout_gravity="left|bottom"
        android:text="Small Text"
        android:padding="7dp"
        android:background="#ffffff"
        android:backgroundTint="@color/primary_dark_material_light"
        android:textColor="@color/primary_text_default_material_light"
        android:visibility="invisible"
        />

</FrameLayout>


